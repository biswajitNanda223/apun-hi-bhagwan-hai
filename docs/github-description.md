# GitHub Repository Description

Use this as the GitHub repository short description:

```text
Production-ready Fastify TypeScript monorepo with REST, gRPC, Prisma, Swagger docs, deployment templates, and strict Git quality gates.
```

Suggested topics:

```text
fastify, typescript, monorepo, prisma, grpc, rest-api, swagger, openapi, docker, kubernetes, clean-architecture, solid
```
